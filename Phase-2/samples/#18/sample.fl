def func()
    return;
end

def func(k , c)
    return k + c;
end

pattern fib(n)
    | (n == 0) = 1
    | (n == 1) = 1
    | (n > 2) = fib.match(n-1) + fib.match(n-2)
;

pattern fib(n)
    | (n == 0) = 2
    | (n == 1) = 3
    |(n > 2) = fib.match(n-1) + fib.match(n-2)
;

def x()
    return;
end

pattern x(n)
    | (n == 0) = 2
    | (n == 1) = 3
    |(n > 2) = fib.match(n-1) + fib.match(n-2)
;

def main()
    puts(1);
end