def f(a, b , c)
    if (a > b)
        if (c < a)
            return "salam";
        end
        return "ahmagh";
    else
        return "hi";
    end
end

def main() 
    str1 = f(20 , 10 , 15);
    str2 = f(10 , 20 , 30);
    puts(str1);
    puts(str2);
end