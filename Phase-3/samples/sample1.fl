def foo(a, b)
    return a+b;
end

def bar(a, b)
    return a-b;
end

def baz(a, b)
    return;
end

def no_return(a, b)
    a+b;
end

def mismatch(a, b)
    if (a > b)
        return a;
    else
        return "b";
    end
end

def main()
    a = 1;
    b = 2;
    c = foo(a, b);
    d = bar(a, b);
    e = baz(a, b);
    f = no_return(a, b);
    g = mismatch(a, b);
    return 0;
end